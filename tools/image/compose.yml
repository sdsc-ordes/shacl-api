services:
  shacl_api:
    environment:
      env_file: .env.dist
    build:
      context: ../..
      dockerfile: ./tools/docker/Dockerfile
      target: webapp
    ports:
      - "8001:15400"
      - "8501:8501"
    volumes:
      - ../../src:/shacl-api/src
    working_dir: /shacl-api
